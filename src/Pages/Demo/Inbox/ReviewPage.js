import React from "react";
import { Typography } from "antd";

const { Text } = Typography;

export const ReviewPage = () => {
    return (
        <Text>
            REVIEW PAGES
        </Text>
    );
};