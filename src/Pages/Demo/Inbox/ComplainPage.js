import React from "react";
import { Typography } from "antd";

const { Text } = Typography;

export const ComplainPages = () => {
    return (
        <Text>
            COMPLAIN PAGE
        </Text>
    );
};