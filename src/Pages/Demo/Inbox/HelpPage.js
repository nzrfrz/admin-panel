import React from "react";
import { Typography } from "antd";

const { Text } = Typography;

export const HelpPage = () => {
    return (
        <Text>
            HELP PAGES
        </Text>
    );
};