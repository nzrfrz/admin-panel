import React from "react";
import { Typography } from "antd";

const { Text } = Typography;

export const ChatPage = () => {
    return (
        <Text>
            CHAT PAGES
        </Text>
    );
};