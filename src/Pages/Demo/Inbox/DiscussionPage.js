import React from "react";
import { Typography } from "antd";

const { Text } = Typography;

export const DiscussionPage = () => {
    return (
        <Text>
            DISCUSSION PAGES
        </Text>
    );
};